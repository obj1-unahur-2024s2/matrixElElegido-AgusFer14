import matrix.*

describe "Test de matriz " {

  test "Hay 3 pasajeros" {
    assert.equals(3, nave.cantidadDePasajeros())
  }

  test "Pasajero con mas vitalidad" {
    assert.equals(morfeo, nave.pasajeroConMasVitalidad())
  }

  test "Esta equilibrada la nave de vitalidad" {
    assert.notThat(nave.hayUnaVitalidadEquilibrada())
  }

  test "Esta el elegido en la nave" {
    assert.that(nave.estaElElegido())
  }

}